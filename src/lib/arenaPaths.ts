// The arena is a static app in public/ — index.html is a file, not a Next
// route, so links to it stay plain <a> elements (no client router involved).
export const ARENA_APP_PATH = "/mulakatmicro1/app/index.html";
export const ARENA_PREVIEW_PATH = "/mulakatmicro1";
